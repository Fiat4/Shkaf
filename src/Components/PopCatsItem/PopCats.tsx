import React from "react";

const PopCatsItem = ({}) => {
	return (
		// <article
		// 	className="category-item"
		// 	onClick={(e) => {
		// 		e.preventDefault();
		// 		// Если кликнули на уже активный элемент - переходим по ссылке
		// 		if (index === activeIndex) {
		// 			const link = item.querySelector("a").getAttribute("href");
		// 			window.location.href = link;
		// 			return;
		// 		}

		// 		// Обновляем активный индекс и позиции
		// 		activeIndex = index;
		// 		updatePositions();
		// 	}}
		// >
		// 	<a href={href} className="category-link">
		// 		<img src={src} alt={alt} className="category-image" loading="lazy" />
		// 	</a>
		// </article>
		<></>
	);
};

export default PopCatsItem;
